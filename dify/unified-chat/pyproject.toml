[project]
name = "unified-chat-router"
version = "0.1.0"
description = "統一チャット(完全bot)のルーティングコア（qa/register/bulk/dedup/pending の振り分け）"
readme = "README.md"
requires-python = ">=3.11"
# 本体は標準ライブラリのみ。Dify Code ノードのサンドボックスでそのまま動かすため外部依存を持たない。
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "ruff>=0.7.0",
    "mypy>=1.13.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "W"]

[tool.ruff.lint.per-file-ignores]
# DSL 合成スクリプトはノード構築の dict リテラルが幅広
"tools/build_dsl.py" = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["router"]
